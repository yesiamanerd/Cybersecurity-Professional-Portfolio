server.name: kibana
server.host: "0.0.0.0"  # Listen on all network interfaces
elasticsearch.hosts: ["http://192.168.1.8:9200"]  # Replace this with the IP address where Elasticsearch is running (in this case, your Windows IP)
